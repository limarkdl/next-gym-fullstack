import React from 'react';

interface DrawerWeightChangeProps {
    name: string;
}

const DrawerWeightChange = (props: DrawerWeightChangeProps) => {
    return (
        <div>

        </div>
    );
};

export default DrawerWeightChange;